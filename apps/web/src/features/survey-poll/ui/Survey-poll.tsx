import React from 'react'

/**
 * @file apps/web/src/features/survey-poll/ui/Survey-poll.tsx
 * @summary survey-poll feature component.
 * @description survey-poll feature UI component.
 */

export interface Survey-pollProps {
  // TODO: Define props
}

export function Survey-poll(props: Survey-pollProps) {
  return (
    <div className="survey-poll-feature">
      <p>Survey-poll Feature</p>
    </div>
  )
}