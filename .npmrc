registry=https://registry.npmjs.org/
# [Task 0.1] node-linker removed — .pnpmrc is authoritative (node-linker=pnpm)
# Rationale: pnpm workspaces require symlink-based resolution; hoisted mode
# conflicts with strict peer dependency enforcement and workspace isolation.
catalogMode=strict
