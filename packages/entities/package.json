{
  "name": "@repo/entities",
  "version": "1.0.0",
  "private": true,
  "description": "FSD v2.1 entities layer - business entities and domain models",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tenant": "./src/tenant/index.ts",
    "./lead": "./src/lead/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:watch": "vitest --watch"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@repo/types": "workspace:*",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "@repo/types": "workspace:*",
    "zod": "catalog:"
  }
}
