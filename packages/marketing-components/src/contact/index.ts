/**
 * @file packages/marketing-components/src/contact/index.ts
 * @role barrel
 * @summary Contact form variant components barrel export
 *
 * Note: Full ContactForm with validation and server actions lives in @repo/features/contact.
 * These are display variants (Standard, Minimal, With Map, etc.) that compose the feature.
 */

export { ContactFormStandard } from './ContactFormStandard';
export type { ContactFormStandardProps } from './ContactFormStandard';
