# clients/ — Codex override

Codex: This directory overrides root AGENTS.md. See [AGENTS.md](AGENTS.md) in this folder for client-specific rules.

- Copy from `starter-template` when creating new clients
- Only change `site.config.ts` and env; no hardcoded client values
- Run `pnpm validate-client clients/<path>` before commit
- No cross-client imports
