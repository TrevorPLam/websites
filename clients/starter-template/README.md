# Starter Template

Golden path client — clone for every new client. Edit `site.config.ts` only.

## Setup

```bash
pnpm install
pnpm dev
```

## Customization

Change `site.config.ts` to customize industry, features, theme, and integrations.
