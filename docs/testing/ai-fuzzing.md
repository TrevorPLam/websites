# AI Fuzzing for Edge Cases

This workflow generates deterministic edge-case payloads to exercise parsers and validators.

## Scope

- String boundary conditions
- Nested JSON depth
- Unicode and control characters

## Command

- `pnpm test:ai-fuzz`
