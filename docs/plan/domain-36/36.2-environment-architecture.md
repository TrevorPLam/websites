### 36.2 Environment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                       ENVIRONMENT CHAIN                         │
│                                                                 │
│  dev (local)          staging (Vercel)        production (Vercel)│
│  ─────────────        ───────────────         ──────────────────│
│  localhost:3000       staging.agency.com      agency.com         │
│  Supabase local       Supabase staging project Supabase prod     │
│  .env.local           .env.staging (Vercel)   .env.production    │
│  No real data         Seeded fixture data      Live tenant data   │
│  Feature flags: all   Feature flags: selective FF: GA-gated       │
│  off by default       testing new features    rollout-ready      │
│                                                                 │
│  Branch: feature/*    Branch: staging         Branch: main       │
│  Deploy: preview PR   Deploy: auto on push    Deploy: promoted   │
└─────────────────────────────────────────────────────────────────┘
```

---
