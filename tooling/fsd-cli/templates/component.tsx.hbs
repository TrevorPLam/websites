import type { FC } from 'react';

export interface {{pascalCase name}}Props {
  className?: string;
}

export const {{pascalCase name}}: FC<{{pascalCase name}}Props> = ({ className }) => {
  return <div className={className}>{{pascalCase name}}</div>;
};
